import { defineStore } from 'pinia'

export const useUserStore = defineStore('store', {
    state() {
        return {
            // userInfo: {}
        }
    },
    actions: {
        // setUserInfo(data) {
        //     this.userInfo = data
        // }
    }
})